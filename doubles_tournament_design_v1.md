# ダブルス交流大会 抽選・得点管理システム 設計書

## 1. 文書情報

- 文書名: ダブルス交流大会 抽選・得点管理システム 設計書
- 版数: 1.0
- 目的: 単一HTMLファイルで動作するWebアプリケーションとして、ダブルス交流大会の抽選、得点入力、ランキング表示を行うシステムの設計を定義する。

---

## 2. 設計方針

### 2.1 基本方針

1. 単一HTMLファイルで完結すること（HTML + CSS + JavaScript）
2. 外部サーバー・データベースを使用しないクライアントサイド完結型とすること
3. GitHub Pagesでそのまま動作し、ローカルでもダブルクリックで開けること
4. 状態は画面内メモリで管理すること（リロードで初期化）
5. 初回抽選は完全乱数、第2試合以降は履歴考慮型抽選とすること
6. 再抽選と確定を明確に分離すること
7. 得点入力・ランキング機能を備えること

### 2.2 技術構成

- 実装形式: 単一HTMLファイル（React不使用、素のHTML + CSS + JavaScript）
- 外部依存: Google Fonts（Zen Kaku Gothic New, Noto Sans JP）のみ
- 永続化: なし（ブラウザリロードで状態リセット）

---

## 3. システム構成

### 3.1 画面構成

タブ切り替えにより以下の2画面を持つ。

- 抽選タブ: 抽選の実行、確定済み履歴の表示、得点入力への導線
- ランキングタブ: 得点集計結果の表示

### 3.2 ハンバーガーメニュー

右上のメニューボタンから以下の機能をモーダルダイアログで呼び出す。

- メンバー管理（初期メンバー一括入力、途中参加追加）
- 参加状態の変更（一時離脱・復帰）
- ペア回数マトリクス（ヒートマップ表示）
- 敵対回数マトリクス（ヒートマップ表示）
- 抽選スコア設定（重み付けパラメータ調整）
- 得点配分設定（勝利点・ラウンド点の設定）
- 履歴リセット（メンバー保持で履歴・得点をクリア）
- 全リセット（すべて初期状態に戻す）

---

## 4. 画面設計

### 4.1 共通ヘッダー

- タイトル表示
- バッジ: 参加中人数、休止中人数、次回試合番号
- ハンバーガーメニューボタン

### 4.2 抽選タブ

#### 4.2.1 メッセージバー

操作結果やガイダンスを1行で表示する。

#### 4.2.2 参加状態パネル

現在の参加中メンバーと休止中メンバーをチップ形式で一覧表示する。長い名前はツールチップで全名を確認可能。

#### 4.2.3 操作ボタン

- 抽選: 参加中メンバー4名以上で有効。押すと候補を生成（初回は完全乱数、2回目以降は履歴考慮）。再度押すと別候補を生成。
- 確定: 現在の候補を確定し、履歴に追加する。

#### 4.2.4 候補表示カード

抽選結果の候補を表示する。表示内容は以下の通り。

- 試合番号、抽選モード（完全乱数/履歴考慮）、総合スコア
- 統計サマリ（新規ペア数、ペア重複数、対戦重複数）
- ペア重複の有無と詳細（過去何回成立したか）
- 対戦カード一覧
- ペア一覧
- 待機者一覧（人数が4の倍数でない場合のみ）
- スコア内訳（各ペナルティ・ボーナスの値）

#### 4.2.5 確定済み履歴

確定済み試合を新しい順に表示する。各試合カードには以下を含む。

- 試合番号、確定日時
- 対戦カード一覧（得点入力済みの場合はスコアとWINバッジを表示）
- ペア一覧
- 待機者・不参加者（該当がある場合のみ、対戦カードの後に表示）
- 得点入力ボタン（モーダルで得点入力画面を開く）
- コピーボタン（試合情報をクリップボードにテキストコピー）

### 4.3 ランキングタブ

#### 4.3.1 表示切り替え

ドロップダウンで「累計」と「第n試合」を切り替え可能。

#### 4.3.2 ランキングテーブル

- 列: 名前、試合数、勝利数、総ラウンド数、ボーナス、合計得点、内訳
- 各列ヘッダーをクリックで昇順・降順・ソート解除（登録メンバー順）の3段階ソート
- デフォルト表示は登録メンバー順

### 4.4 モーダルダイアログ

#### 4.4.1 メンバー管理

- テキストエリアによる初期メンバー一括入力（1行1名）
- 途中参加メンバーの個別追加
- 初期メンバー反映時は履歴がリセットされる

#### 4.4.2 参加状態の変更

- 参加中メンバー一覧（一時離脱ボタン付き）
- 休止中メンバー一覧（復帰ボタン付き）

#### 4.4.3 ペア回数マトリクス / 敵対回数マトリクス

- 全メンバー × 全メンバーの対称行列
- 値に応じた赤系グラデーションのヒートマップ表示
- 休止中メンバーにはバッジ表示
- スクロール可能、ヘッダー・行頭固定

#### 4.4.4 抽選スコア設定

4グループに分類された全22パラメータを数値入力で調整可能。変更は次回抽選から反映。

#### 4.4.5 得点配分設定

- 1勝あたりの得点（初期値: 0）
- 1ラウンドあたりの得点（初期値: 1）
- 変更は即座にランキングに反映

#### 4.4.6 得点入力

- 確定済み試合ごとにモーダルで開く
- 各対戦カードの両チームの取得ラウンド数を入力
- ラウンド数が多い方に自動でWIN表示
- 各選手ごとのボーナス得点入力欄

#### 4.4.7 リセット確認

- カスタムダイアログ（ブラウザのconfirmを使わず、ページ内オーバーレイ）
- 「キャンセル」「実行」の2ボタン、背景クリックでキャンセル

---

## 5. 状態設計

ブラウザ内のJavaScript変数で管理する（リロードで初期化）。

### 5.1 状態一覧

| 変数名 | 型 | 内容 |
|--------|------|------|
| P | Participant[] | 参加者一覧 |
| CR | RoundRecord[] | 確定済み試合履歴 |
| CC | RoundCandidate \| null | 現在の未確定候補 |
| PI | string | テキストエリア入力文字列 |
| RS | Record<number, Record<number, ScoreData>> | 得点データ（試合番号 → カード番号 → スコア） |
| W | DrawWeights | 抽選重みパラメータ |
| SC | ScoreConfig | 得点配分設定 |
| rankSort | {col, dir} | ランキングのソート状態 |
| rankFilter | string | ランキングの表示フィルタ（"all" or 試合番号） |

---

## 6. データ構造設計

### 6.1 Participant

```
{ id: string, name: string, st: "a" | "i" }
```

- id: `p-{連番}-{名前}` の形式。内部識別に使用。
- st: "a" = 参加中（active）、"i" = 休止中（inactive）

### 6.2 Team

```
{ p: [string, string] }
```

プレイヤーIDの2要素配列。

### 6.3 MatchCard

```
{ a: Team, b: Team }
```

### 6.4 RoundCandidate

```
{
  rn: number,           // 試合番号
  mode: "r" | "o",      // r=完全乱数, o=履歴考慮
  ai: string[],         // 出場者ID
  wi: string[],         // 待機者ID
  teams: Team[],        // ペア一覧
  mc: MatchCard[],      // 対戦カード一覧
  score: number,        // 総合スコア（小さいほど良い）
  sbd: ScoreBreakdown,  // スコア内訳
  sum: CandidateSummary, // 重複・新規の集計
  rpD: RepeatedPairDetail[], // 重複ペア詳細
  fp: string            // fingerprint（重複排除用）
}
```

### 6.5 RoundRecord

```
{
  rn: number,           // 試合番号
  at: string,           // 確定日時
  ai: string[],         // 出場者ID
  ii: string[],         // 不参加者ID
  wi: string[],         // 待機者ID
  teams: Team[],
  mc: MatchCard[]
}
```

### 6.6 ScoreData（得点データ）

```
{
  ar: number | "",      // チームAの取得ラウンド数
  br: number | "",      // チームBの取得ラウンド数
  bonus: { [playerId]: number }  // 各選手のボーナス得点
}
```

### 6.7 HistoryStats

```
{
  pc: Record<string, number>,       // ペア組み合わせ回数
  oc: Record<string, number>,       // 敵対組み合わせ回数
  plc: Record<string, number>,      // 出場回数
  bc: Record<string, number>,       // 待機回数
  ps: Record<string, Set<string>>,  // 過去パートナー集合
  os: Record<string, Set<string>>,  // 過去対戦相手集合
  rpc: Record<string, number>,      // 直近ペア回数（直近2回分）
  roc: Record<string, number>,      // 直近対戦回数（直近2回分）
  mcc: Record<string, number>       // 対戦カード単位の重複回数
}
```

### 6.8 キー設計

- ペアキー / 対戦キー: `min(id1,id2)|max(id1,id2)` — 順序に依存しない
- 対戦カードキー: `min(teamKey1,teamKey2)vs max(teamKey1,teamKey2)` — チーム順序・選手順序に依存しない

---

## 7. 抽選アルゴリズム設計

### 7.1 初回抽選（完全乱数）

適用条件: `confirmedRounds.length === 0`

1. 全参加者をシャッフル
2. `参加者数 % 4` 人を待機者とする
3. 残りを2名ずつペア化
4. ペアをシャッフルし、2ペアずつ対戦カード化

### 7.2 履歴考慮抽選

適用条件: `confirmedRounds.length >= 1`

1. HistoryStatsを構築
2. 2,500回（設定変更可）の候補生成ループ
3. 各候補で以下を実行:
   a. 待機者選定（待機回数偏り考慮）
   b. greedy方式でペア生成（交流多様性考慮）
   c. 局所改善（ペア組み替え試行）
   d. 対戦カード生成（全組み合わせ列挙＋評価）
   e. スコア計算
4. fingerprint（対戦構成の正規化文字列）で重複排除
5. スコア上位K件を保持
6. 最良候補を表示

### 7.3 待機者選定

待機優先スコアを計算し、重み付きランダムで選択する。

```
byeScore(p) =
  byeCount[p] × byeWeight
  - playCount[p]
  - unusedPartners[p] × unusedPartnerWeight
  - unusedOpponents[p] × unusedOpponentWeight
  + smallRandom
```

スコアが小さい（＝待機すべき度が低い）ほど選ばれにくい。

### 7.4 ペア生成（greedy方式）

1. 未割当の中から「選択肢が少ない参加者」を優先してアンカーに選ぶ
2. アンカーに対し、全候補をスコア評価し、重み付きランダムで相方を選ぶ

```
pairChoiceScore(a, b) =
  pairCount[a,b] × greedyPairWeight
  + recentPairCount[a,b] × greedyRecentPairWeight
  - isNewPair(a,b) × greedyNewPairBonus
  - (diversityGain) × greedyDiversityWeight
  + smallRandom
```

### 7.5 局所改善

2ペアを選び、メンバー組み替え2パターンを試す。スコア改善があれば採用。設定回数分繰り返す。

### 7.6 対戦カード生成

ペア確定後、全対戦組み合わせを列挙し、敵対履歴スコアが最小の組み方を採用する。

### 7.7 スコア関数

```
score =
  pairPenalty          (10,000 × 過去ペア回数)
+ recentPairPenalty    (25,000 × 直近ペア回数)
+ opponentPenalty      (350 × 過去敵対回数)
+ recentOpponentPenalty(1,800 × 直近敵対回数)
+ matchCardPenalty     (20,000 × 同一カード回数)
+ byePenalty           (80 × 待機回数)
+ playPenalty          (10 × 出場偏り)
+ fairnessPenalty      (10 × 出場回数分散 + 18 × 交流多様性不足)
- newPairBonus         (1,200 × 新規ペア数)
- newOpponentBonus     (120 × 新規敵対数)
```

スコアは小さいほど良い。全パラメータは設定画面から変更可能。

重み付け優先順位:
1. 直近ペア重複回避（25,000）
2. 同一対戦カード重複回避（20,000）
3. 同一ペア重複回避（10,000）
4. 直近対戦相手重複回避（1,800）
5. 新規ペア促進（1,200）
6. 対戦相手重複回避（350）
7. 新規対戦促進（120）
8. 待機・出場公平性（80/10）

---

## 8. 得点システム設計

### 8.1 勝敗判定

各対戦カードについて、両チームの取得ラウンド数を入力する。ラウンド数が多い方が勝利。同数の場合は引き分け（勝利なし）。

### 8.2 個人得点計算

```
個人得点 = 勝利数 × 勝利得点 + 総ラウンド数 × ラウンド得点 + ボーナス得点
```

- 勝利得点の初期値: 0pt/勝
- ラウンド得点の初期値: 1pt/R
- ボーナス得点: 手動入力

### 8.3 ランキング表示

- 全メンバーの得点を集計し、テーブル表示
- 累計 / 各試合ごとの切り替え表示
- 各列で昇順・降順・ソート解除の3段階ソート（デフォルトは登録メンバー順）

---

## 9. クリップボードコピー

確定済み試合の情報をテキスト形式でコピーする。Artifact環境対応のため、`navigator.clipboard.writeText` 失敗時は `document.execCommand('copy')` にフォールバックする。

コピーされるテキスト形式:
```
第1試合（2025/01/15 14:30:00）

【対戦カード】
1. 田中 / 佐藤  vs  鈴木 / 高橋  (3-1)

【ペア】
1. 田中 / 佐藤
2. 鈴木 / 高橋

待機: 伊藤
```

---

## 10. マトリクス表示

ペア回数・敵対回数を全メンバー × 全メンバーの対称行列で表示する。

- 値0のセルは無色
- 値が大きいほど赤が濃くなるヒートマップ表示
- 色の濃さは `rgba(192,57,43, 0.08 + (value/maxValue) × 0.45)` で算出
- 値が最大値の50%を超えるセルは白文字に切り替え
- 対角線セルは「-」表示
- 休止中メンバーには「休止」バッジ表示

---

## 11. リセット処理

### 11.1 履歴リセット

- 確定済み履歴、得点データ、現在候補をクリア
- メンバー登録と参加状態は保持

### 11.2 全リセット

- メンバー登録を初期値に戻す
- 確定済み履歴、得点データ、現在候補をクリア
- 参加状態もすべて「参加中」に戻る

いずれもカスタム確認ダイアログを表示し、明示的な「実行」ボタン押下が必要。

---

## 12. バリデーション

- 参加中メンバーが4名未満の場合、抽選ボタン無効化
- 確定する候補がない場合、確定ボタン無効化
- 得点入力は数値のみ（type=number）
- 参加者名の前後空白はtrim、空行は無視

---

## 13. 実装上の留意事項

1. ペアキーは順序非依存（`min|max`形式）で、「A/B」と「B/A」は同一ペアとして扱う
2. 対戦カードキーも順序非依存で、同一の2ペアの対戦は常に同一カードとして扱う
3. Google Fontsへのネットワークアクセスがない環境ではシステムフォントにフォールバック
4. ブラウザリロードで全状態が初期化される（永続化機能は未実装）
5. 同名参加者がいても内部IDで区別される
